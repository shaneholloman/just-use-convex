{
  "name": "@just-use-convex/backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "scripts": {
    "dev": "convex dev",
    "dev:setup": "convex dev --configure --until-success",
    "build": "tsc --noEmit --project convex",
    "check-types": "tsc --noEmit --project convex"
  },
  "dependencies": {
    "@convex-dev/aggregate": "^0.2.1",
    "@convex-dev/better-auth": "catalog:",
    "@convex-dev/migrations": "^0.3.1",
    "better-auth": "catalog:",
    "convex": "catalog:",
    "convex-ents": "^0.18.1",
    "convex-helpers": "^0.1.111",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@just-use-convex/config": "workspace:*",
    "@types/node": "^24.3.0",
    "typescript": "catalog:"
  }
}
